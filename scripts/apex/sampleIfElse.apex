string s1 = '';

//if(string.isBlank(s1))

//or

if(s1 ==''){
    system.debug('s1 is blank.okay?');
}else{
    system.debug(s1);
}

if(string.isBlank(s1)){
    system.debug('s1 is still blank');
}else{
    system.debug(s1);
}